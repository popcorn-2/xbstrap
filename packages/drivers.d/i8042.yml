sources:
  - name: i8042
    git: 'https://github.com/popcorn-2/i8042.git'

packages:
  - name: i8042
    source: i8042
    require-tools:
      - rust
      - llvm
      - devman-update-registry
    build: >
      env RUSTC=@@TOOLCHAIN@@/bin/rustc
      @@TOOLCHAIN@@/bin/cargo rustc
      --target @@TRIPLE@@
      --target-dir .
      --manifest-path @@SOURCES@@/Cargo.toml
      --release
      --
      -C linker=@@TOOLCHAIN@@/bin/clang
      -C link-arg=--target=@@TRIPLE@@
      -C link-arg=--sysroot=@@SYSROOT@@
      -C panic=abort
      -C target-feature=+crt-static
    # -C target-cpu=native
    install:
      - mkdir -p @@INSTALL_DIR@@/driver/
      - install @@TRIPLE@@/release/i8042.exec @@INSTALL_DIR@@/driver/i8042.exec
      - '@@TOOLCHAIN@@/system/bin/update-driver-registry @@SYSROOT@@'
