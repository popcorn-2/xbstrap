sources:
  - name: i8042
    git: 'https://github.com/popcorn-2/i8042.git'

packages:
  - name: i8042
    source: i8042
    require-tools:
      - rust
      - llvm
      - devman-update-registry
    build: >
      env RUSTC=@@TOOLCHAIN@@/system/bin/rustc
      @@TOOLCHAIN@@/system/bin/cargo rustc
      --target @@TRIPLE@@
      --target-dir .
      --manifest-path @@SOURCES@@/Cargo.toml
      --release
      --
      -C linker=@@TOOLCHAIN@@/system/bin/clang
      -C link-arg=--target=@@TRIPLE@@
      -C link-arg=--sysroot=@@SYSROOT@@
      -C panic=abort
      -C target-feature=+crt-static
    # -C target-cpu=native
    install:
      - mkdir -p @@SYSROOT@@/system/bin/driver/
      - install @@TRIPLE@@/release/i8042.exec @@SYSROOT@@/system/bin/driver/i8042.exec
      - '@@TOOLCHAIN@@/system/bin/update-driver-registry @@SYSROOT@@'
