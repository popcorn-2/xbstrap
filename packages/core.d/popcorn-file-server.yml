sources:
  - name: popcorn-file-server
    git: 'https://github.com/popcorn-2/file-server.git'

packages:
  - name: file-server
    source: popcorn-file-server
    require-tools:
      - rust
      - llvm
    build: >
      env RUSTC=@@TOOLCHAIN@@/system/bin/rustc
      @@TOOLCHAIN@@/system/bin/cargo build
      --target @@TRIPLE@@
      --target-dir .
      --manifest-path @@SOURCES@@/Cargo.toml
      --release
      -C linker=@@TOOLCHAIN@@/system/bin/clang
      -C link-arg=--target=@@TRIPLE@@
      -C link-arg=--sysroot=@@SYSROOT@@
      -C panic=abort
      -C target-feature=+crt-static
    # -C target-cpu=native
    install:
      - mkdir -p @@SYSROOT@@/system/bin/
      - install @@TRIPLE@@/release/file-server.exec @@SYSROOT@@/system/bin/file-server.exec
